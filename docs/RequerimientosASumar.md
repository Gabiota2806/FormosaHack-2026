Requerimientos a sumar:
    ->  Agregar el uso de IA en el chatbot.
    ->  cubrirnos del prompt injected.
    ->  Solucionar problemas de diseño en el modal de SOS, la barra de scroll rompe con el estilo redondeado del modal.
    ->  Tenemos que crear una landing page donde contenga toda la información del sistema.
    ->  El chatbot tiene que estar dentro de un widget flotante en la parte inferior derecha de la pantalla, siempre tiene que estar visible y una vez que se hace click en él se desplega de forma dinámica el chatbot partiendo desde el widget.
    ->  Tenemos que dar la posibilidad de que el sistema pueda mandar notificaciones push en los teléfonos cuando se detecten brotes de estafas.
    ->  El sistema tiene que tener un modo abuelo ya que las personas de la tercera edad son lo que más sufren de estafas virtuales, este modo tiene que aumentar el tamaño de las letras y simplificar significativamente para que el uso del sistema genere la menor recistencia posible.
    ->  Acomodar mejor el módulo de login para que tengamos un botón de usuario, y que el chat pueda funcionar tanto sin logueo, pero cuando se loguea se puede mantener un historial de chats.
